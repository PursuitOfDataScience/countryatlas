% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sources.R
\name{compare_sources}
\alias{compare_sources}
\title{Do two sources agree?}
\usage{
compare_sources(
  indicator,
  sources = c("wdi", "owid"),
  year,
  countries = NULL,
  tolerance = 0.05
)
}
\arguments{
\item{indicator}{Either one indicator code used for every source, or a named
character vector giving each source its own code:
\code{c(wdi = "NY.GDP.PCAP.KD", owid = "gdp_per_capita")}.}

\item{sources}{Source names to compare (default \code{c("wdi", "owid")}).}

\item{year}{A single year.}

\item{countries}{Optional \code{iso3c} subset.}

\item{tolerance}{Relative difference above which a country counts as a
disagreement (default \code{0.05}, i.e. 5\%).}
}
\value{
A tibble with one row per country: the value from each source,
\code{n_sources} (how many reported it), \code{rel_diff} (max relative spread) and
\code{disagrees}. The correlation, coverage and disagreement summary is attached
as the \code{"countryatlas_source_summary"} attribute.
}
\description{
Fetch the same indicator from several providers and put the answers side by
side. Sources disagree more often than people expect -- different vintages,
PPP bases, territorial definitions, revision schedules -- and on the ISO spine
the comparison is one join. This is the verb that turns "I used OWID" into "I
used OWID, and here is where it differs from the World Bank".
}
\examples{
\dontrun{
cmp <- compare_sources(c(wdi = "NY.GDP.PCAP.KD", owid = "gdp_per_capita"),
                       sources = c("wdi", "owid"), year = 2020)
attr(cmp, "countryatlas_source_summary")
}
}
\seealso{
\code{\link[=fetch_indicator]{fetch_indicator()}}, \code{\link[=country_sources]{country_sources()}}
}
