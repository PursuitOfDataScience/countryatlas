% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualization.R
\name{gridded_cartogram}
\alias{gridded_cartogram}
\title{One square per N people}
\usage{
gridded_cartogram(data, value, cells = 1000, fill = NULL, cell_size = 2.5)
}
\arguments{
\item{data}{A country-level or map-ready frame with \code{iso3c}.}

\item{value}{The column to allocate cells by (unquoted).}

\item{cells}{Total number of cells to distribute (default \code{1000}). Each cell
is then worth \code{sum(value) / cells}.}

\item{fill}{Optional fill column (unquoted); defaults to \code{value}.}

\item{cell_size}{Grid spacing in degrees (default \code{2.5}).}
}
\value{
A \code{ggplot} object. The per-country cell allocation is attached as the
\code{"countryatlas_cells"} attribute -- every placeable country, including the
ones that rounded to zero cells, so \code{share} sums to 1 and the rounding is
fully visible.
}
\description{
A gridded (or "waffle") cartogram: the world redrawn as equal cells, each
worth a fixed quantity, allocated to countries in proportion to their value
and placed near where they belong. Where a Dorling cartogram preserves
position and a contiguous one preserves adjacency, this preserves
\emph{countability} -- the reader can literally count the cells.
}
\section{Rounding is the whole difficulty}{

Allocating a whole number of cells to each country cannot be exact, so the
remainder has to go somewhere. This uses the largest-remainder method, which
guarantees the cell total is exactly \code{cells} and that no country with a
positive value gets zero cells while a smaller one gets one. The attached
table reports each country's exact share alongside its integer allocation so
the rounding is inspectable rather than hidden.
}

\section{Crowded neighbours overlap}{

Each country's block is centred on its own centroid, with no collision
avoidance between countries. That is deliberate -- a global packing solve
would push countries away from where they belong -- but it means blocks in
crowded regions are drawn on top of one another, and a partly hidden block
cannot be counted or compared. The effect is not marginal: at the defaults
(\code{cells = 1000}, \code{cell_size = 2.5}) about a third of the cells overlap a
cell of a different country, across some sixty countries, and it grows with
\code{cells} -- at \code{cells = 2500} it is roughly two thirds.

\code{cell_size} is the lever, because it scales the tiles without moving the
centroids: dropping it to \code{1.5} cuts the overlap at \code{cells = 1000} to about
a tenth of the cells. Fewer \code{cells} also helps. Where exact areas matter
more than geographic position, \code{\link[=dorling_map]{dorling_map()}} resolves collisions by
displacing circles instead.
}

\examples{
\donttest{
snap <- countryatlas::world_snapshot$countries
gridded_cartogram(snap, population, cells = 400)
}
}
\seealso{
\code{\link[=cartogram_map]{cartogram_map()}}, \code{\link[=dorling_map]{dorling_map()}}, \code{\link[=tile_map]{tile_map()}}
}
