% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/provenance.R
\name{map_provenance}
\alias{map_provenance}
\title{What went into this map}
\usage{
map_provenance(x, value = NULL)
}
\arguments{
\item{x}{A plot returned by any of the package's map verbs -- \code{\link[=world_map]{world_map()}},
\code{\link[=bubble_map]{bubble_map()}}, \code{\link[=spike_map]{spike_map()}}, \code{\link[=tile_map]{tile_map()}}, \code{\link[=flow_map]{flow_map()}}, \code{\link[=globe_map]{globe_map()}},
\code{\link[=bivariate_map]{bivariate_map()}}, \code{\link[=cartogram_map]{cartogram_map()}}, \code{\link[=dorling_map]{dorling_map()}},
\code{\link[=gridded_cartogram]{gridded_cartogram()}}, \code{\link[=value_by_alpha_map]{value_by_alpha_map()}}, \code{\link[=coverage_map]{coverage_map()}},
\code{\link[=classify_compare]{classify_compare()}}, \code{\link[=facet_map]{facet_map()}} or \code{\link[=lisa_map]{lisa_map()}} -- or a map-ready data
frame, for which the data-side facts are reported and the drawing-side ones
are \code{NA}.}

\item{value}{For a data frame, the column whose coverage to report
(unquoted). Ignored for a plot, which already knows its own fill.}
}
\value{
A one-row tibble of provenance fields, invisibly printed in a
human-readable block. Fields: \code{countryatlas}, \code{fill}, \code{backend},
\code{projection}, \code{style}, \code{n_bins}, \code{na_style}, \code{n_countries}, \code{n_missing},
\code{n_total}, \code{uncertainty}, \code{disputes}, \code{dispute_policy}, \code{n_imputed},
\code{breaks}, \code{missing_iso3c} and \code{snapshot_year}.

The three counts are: \code{n_countries}, the countries actually drawn with a
value; \code{n_missing}, those drawn without one; and \code{n_total}, the two added
together -- every country the map covers. \code{n_countries} is the numerator,
not the denominator, which its name does not say on its own.
}
\description{
Report the provenance of a \code{\link[=world_map]{world_map()}} (or any plot the package's map verbs
produced): the package version, the geometry backend and projection, the
classification method and its breaks, the fill column, and how many countries
are shown versus missing. These are the questions a reviewer asks first, and
the answers are already known at plot time -- this just makes them readable.
}
\section{Putting it on the plot}{

\code{\link[=world_map]{world_map()}}\code{(footnote = "auto")} prints the coverage line as a caption, and
\code{classification_report = TRUE} attaches the per-class counts. Together they
cover what a methods note needs:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{p <- world_map(mapdf, gdp_per_capita, style = "quantile",
               footnote = "auto", classification_report = TRUE)
map_provenance(p)
attr(p, "countryatlas_classification")
}\if{html}{\out{</div>}}
}

\examples{
\donttest{
snap <- countryatlas::world_snapshot$countries
if (requireNamespace("maps", quietly = TRUE)) {
  p <- attach_geometry(snap, geometry = "polygon") |>
    world_map(gdp_per_capita, style = "quantile")
  map_provenance(p)
}
}
}
\seealso{
\code{\link[=world_map]{world_map()}}, \code{\link[=audit_coverage]{audit_coverage()}}, \code{\link[=coverage_map]{coverage_map()}}
}
