% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial-stats.R
\name{gearys_c}
\alias{gearys_c}
\title{Geary's C (spatial autocorrelation)}
\usage{
gearys_c(data, value, weights = NULL, n_perm = 999)
}
\arguments{
\item{data}{A country-level frame with \code{iso3c} and the value column.}

\item{value}{The value column (unquoted).}

\item{weights}{A \code{\link[=country_weights]{country_weights()}} object. Defaults to land-border
contiguity, which excludes islands -- prefer \code{country_weights("knn")} for
global work.}

\item{n_perm}{Permutations for the pseudo-p-value (default \code{999}; use \code{0} to
skip the test, which leaves \code{p_value} as \code{NA}).}
}
\value{
A one-row tibble: \code{c} (observed), \code{expected} (always 1), \code{n}
(countries used), \code{n_excluded} (countries with data that the weights could
not reach), \code{n_links} (non-zero weights), \code{p_value} and an \code{excluded}
list-column of the excluded \code{iso3c} codes. \code{n} and \code{n_excluded} sum to the
countries supplied with a value, and mean the same here as in
\code{\link[=morans_i]{morans_i()}}.

\code{p_value} is \strong{one-sided on the lower tail}:
\eqn{(1 + \#\{C^{*} \le C_{obs}\}) / (n_{perm} + 1)}. The lower tail is
the clustered one, which is the opposite way round from Moran's \emph{I}:
Geary's \emph{C} runs from 0 (neighbours identical) through 1 (no
autocorrelation) upwards, so a \emph{small} \code{c} is the evidence of positive
spatial association. Never exactly zero; the floor is
\eqn{1/(n_{perm}+1)}. Set a seed beforehand for a reproducible \code{p_value}.
}
\description{
The other classical global autocorrelation statistic. Where Moran's I is a
correlation-like measure centred on \eqn{-1/(n-1)}, Geary's C is a
distance-like one centred on 1: \strong{below 1} means positive autocorrelation
(neighbours are similar), above 1 means negative. It is more sensitive than
Moran's I to local differences.
}
\examples{
\donttest{
snap <- countryatlas::world_snapshot$countries
gearys_c(snap, gdp_per_capita, weights = country_weights("knn", k = 5),
         n_perm = 99)
}
}
\references{
Geary, R. C. (1954). The contiguity ratio and statistical mapping.
\emph{The Incorporated Statistician} 5(3), 115-146. \doi{10.2307/2986645}
}
\seealso{
\code{\link[=morans_i]{morans_i()}}, \code{\link[=country_weights]{country_weights()}}
}
