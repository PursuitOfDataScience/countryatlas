% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/projections.R
\name{projection_compare}
\alias{projection_compare}
\title{The same map under several projections}
\usage{
projection_compare(
  data,
  fill,
  projections = c("equal_earth", "robinson", "winkel_tripel", "mercator"),
  ncol = NULL,
  labeller = c("name", "property"),
  ...
)
}
\arguments{
\item{data}{An \code{sf} map-ready frame (projections are an \code{sf}-backend
feature; the polygon backend draws in \code{coord_quickmap()} and cannot
reproject).}

\item{fill}{The fill column (unquoted).}

\item{projections}{Projections to compare (default: Equal Earth, Robinson,
Winkel tripel and Mercator -- an equal-area, two compromises and a
conformal). See \code{\link[=projection_info]{projection_info()}}.}

\item{ncol}{Number of facet columns.}

\item{labeller}{\code{"name"} (default) labels each panel with the projection
name; \code{"property"} appends what it preserves, which is the point of the
comparison.}

\item{...}{Passed to \code{\link[=world_map]{world_map()}} (e.g. \code{style}, \code{palette}, \code{n_bins}).}
}
\value{
A faceted \code{ggplot} object.
}
\description{
Small multiples of one choropleth, drawn once per projection, so the cost of
a projection choice is visible rather than asserted. The data and the
classification are held fixed; only the CRS varies.
}
\examples{
\donttest{
if (requireNamespace("sf", quietly = TRUE) &&
    requireNamespace("rnaturalearth", quietly = TRUE)) {
  attach_geometry(countryatlas::world_snapshot$countries, geometry = "sf") |>
    projection_compare(gdp_per_capita, style = "quantile")
}
}
}
\seealso{
\code{\link[=projection_info]{projection_info()}}, \code{\link[=tissot_map]{tissot_map()}}
}
