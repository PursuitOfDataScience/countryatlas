% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/networks.R
\name{od_map}
\alias{od_map}
\title{Origin-destination small multiples}
\usage{
od_map(
  data,
  from,
  to,
  weight = NULL,
  origin = "country.name",
  origins = 6,
  direction = c("out", "in"),
  ...
)
}
\arguments{
\item{data}{An OD table.}

\item{from, to}{The origin and destination country columns (unquoted).}

\item{weight}{The flow column (unquoted). If omitted, every pair counts as 1.}

\item{origin}{How to read \code{from}/\code{to} (default \code{"country.name"}).}

\item{origins}{Which origins to draw. A character vector of names or codes,
or an integer giving how many of the largest to take (default \code{6}).}

\item{direction}{\code{"out"} (default; one panel per origin, showing
destinations) or \code{"in"} (one panel per destination, showing origins).}

\item{...}{Passed to \code{\link[=world_map]{world_map()}}.}
}
\value{
A faceted \code{ggplot} object.
}
\description{
One small map per origin, each showing where that origin's flow goes. The
answer to the arc map's central problem: past a few dozen flows,
\code{\link[=flow_map]{flow_map()}} is a plate of spaghetti and an OD map is legible.
}
\examples{
\donttest{
od <- data.frame(
  from = rep(c("China", "Germany", "USA"), each = 3),
  to   = c("USA", "Japan", "Brazil", "France", "Italy", "Poland",
           "Mexico", "Canada", "Japan"),
  value = c(500, 200, 90, 80, 70, 60, 300, 280, 120)
)
if (requireNamespace("maps", quietly = TRUE)) {
  od_map(od, from, to, value, origins = 3)
}
}
}
\seealso{
\code{\link[=flow_map]{flow_map()}}, \code{\link[=country_network]{country_network()}}, \code{\link[=facet_map]{facet_map()}}
}
