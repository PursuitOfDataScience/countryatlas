% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/join.R
\name{country_join_all}
\alias{country_join_all}
\title{Join many messy country tables on the ISO spine}
\usage{
country_join_all(
  tables,
  by,
  origin = "country.name",
  type = c("full", "left", "inner"),
  key = c("iso3c", "cowc", "cown", "gwn"),
  warn = TRUE
)
}
\arguments{
\item{tables}{A list of data frames.}

\item{by}{A single country-column name present in every table, or a character
vector giving the column for each table.}

\item{origin}{countrycode origin scheme(s) for the key column(s) (default
\code{"country.name"}; length 1 or one per table).}

\item{type}{Join type: \code{"full"} (default), \code{"left"} or \code{"inner"}.}

\item{key}{Which code system to join on, as in \code{\link[=country_join]{country_join()}}: \code{"iso3c"}
(default), or \code{"cowc"}/\code{"cown"}/\code{"gwn"} for historical work that predates
ISO 3166. Each table reports separately on the countries the alternate key
cannot carry.}

\item{warn}{Whether to report values that resolve to no country (default
\code{TRUE}), per table, as \code{\link[=country_join]{country_join()}} does per side.}
}
\value{
A single tibble joined on \code{key} (clashing non-key columns get
dplyr's default \code{.x}/\code{.y} suffixes).
}
\description{
The many-table generalisation of \code{\link[=country_join]{country_join()}}: reduce-join a list of data
frames that each key on country names or codes, reconciling every one to
\code{iso3c} first.
}
\examples{
a <- data.frame(country = c("Czechia", "South Korea"), gdp = c(1, 2))
b <- data.frame(country = c("Czech Republic", "Korea, Rep."), pop = c(10, 51))
d <- data.frame(country = c("Czechia", "Korea"), area = c(79, 100))
country_join_all(list(a, b, d), by = "country")
}
