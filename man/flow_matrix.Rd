% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/networks.R
\name{flow_matrix}
\alias{flow_matrix}
\title{An origin-destination table as a matrix}
\usage{
flow_matrix(
  data,
  from,
  to,
  weight = NULL,
  origin = "country.name",
  symmetric = FALSE,
  fill = 0
)
}
\arguments{
\item{data}{An OD table.}

\item{from, to}{The origin and destination country columns (unquoted).}

\item{weight}{The flow column (unquoted). If omitted, every pair counts as 1.}

\item{origin}{How to read \code{from}/\code{to} (default \code{"country.name"}).}

\item{symmetric}{If \code{TRUE}, add the transpose so the matrix is undirected
(default \code{FALSE}).}

\item{fill}{Value for pairs with no flow (default \code{0}).}
}
\value{
A square numeric matrix with \code{iso3c} row and column names. Rows are
origins, columns destinations.
}
\description{
Reshape a long bilateral table (trade, migration, flights, remittances) into
a square origin x destination matrix on the ISO spine, with both country
columns standardised. The natural input to a network analysis, and to
\code{\link[=country_weights]{country_weights()}}\code{(type = "custom")} -- which is how "countries near each
other in trade space" becomes a spatial weights object.
}
\examples{
od <- data.frame(
  from = c("China", "China", "Germany", "USA"),
  to   = c("USA", "Germany", "France", "Mexico"),
  value = c(500, 100, 80, 300)
)
flow_matrix(od, from, to, value)
}
\seealso{
\code{\link[=country_network]{country_network()}}, \code{\link[=flow_map]{flow_map()}}, \code{\link[=country_weights]{country_weights()}}
}
