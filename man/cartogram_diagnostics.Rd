% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualization.R
\name{cartogram_diagnostics}
\alias{cartogram_diagnostics}
\title{Did the cartogram actually converge?}
\usage{
cartogram_diagnostics(x, weight = NULL)
}
\arguments{
\item{x}{A \code{ggplot} from \code{\link[=cartogram_map]{cartogram_map()}} or \code{\link[=dorling_map]{dorling_map()}}, or the \code{sf}
frame the cartogram was computed from.}

\item{weight}{The weight column (unquoted). Required when \code{x} is a plain \code{sf}
frame; read from the plot otherwise.}
}
\value{
A tibble of \code{iso3c}, \code{target_share} (the country's share of the
weight), \code{actual_share} (its share of the cartogram's area) and
\code{area_error} (the relative difference). The summary -- mean absolute error,
worst country -- is attached as the \code{"countryatlas_cartogram"} attribute.
}
\description{
Cartograms fail quietly. An under-converged one looks entirely plausible
while still misrepresenting the areas it exists to make honest. This reports
the residual error per country, so the failure is visible.
}
\section{What counts as converged}{

A perfect cartogram has \code{area_error} of 0 everywhere. In practice a mean
absolute error under a few percent is good and under 10\% is usually
acceptable; a systematically large error, or one concentrated in the small
countries, means the algorithm stopped early. Raise \code{itermax} and try again.
}

\examples{
\donttest{
if (requireNamespace("sf", quietly = TRUE) &&
    requireNamespace("cartogram", quietly = TRUE) &&
    requireNamespace("rnaturalearth", quietly = TRUE)) {
  sfd <- attach_geometry(countryatlas::world_snapshot$countries,
                         geometry = "sf")
  cg <- cartogram_map(sfd, population)
  cartogram_diagnostics(cg)
}
}
}
\seealso{
\code{\link[=cartogram_map]{cartogram_map()}}, \code{\link[=dorling_map]{dorling_map()}}, \code{\link[=gridded_cartogram]{gridded_cartogram()}}
}
