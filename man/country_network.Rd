% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/networks.R
\name{country_network}
\alias{country_network}
\title{Describe an origin-destination table as a network}
\usage{
country_network(
  data,
  from,
  to,
  weight = NULL,
  origin = "country.name",
  top_n = 20
)
}
\arguments{
\item{data}{An OD table.}

\item{from, to}{The origin and destination country columns (unquoted).}

\item{weight}{The flow column (unquoted). If omitted, every pair counts as 1.}

\item{origin}{How to read \code{from}/\code{to} (default \code{"country.name"}).}

\item{top_n}{How many edges to return in the \code{edges} element (default \code{20},
largest first). \code{Inf} for all.}
}
\value{
A list of two tibbles:
\itemize{
\item \code{nodes} -- \code{iso3c}, \code{country}, \code{out_flow}, \code{in_flow}, \code{net_flow},
\code{out_degree}, \code{in_degree} and \code{strength_share} (the country's share of
all flow, in or out).
\item \code{edges} -- \code{from}, \code{to}, \code{weight}, \code{share} and \code{reciprocity} (the
opposite flow as a proportion of this one; \code{NA} where there is none).
}
}
\description{
Node- and edge-level summaries of a bilateral flow table: who sends, who
receives, who is central. No \code{igraph} required -- at country scale the dense
arithmetic is trivial.
}
\examples{
od <- data.frame(
  from = c("China", "China", "Germany", "USA", "Mexico"),
  to   = c("USA", "Germany", "France", "Mexico", "USA"),
  value = c(500, 100, 80, 300, 320)
)
country_network(od, from, to, value)
}
\seealso{
\code{\link[=flow_matrix]{flow_matrix()}}, \code{\link[=od_map]{od_map()}}, \code{\link[=flow_map]{flow_map()}}
}
