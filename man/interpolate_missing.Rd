% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/disputes.R
\name{interpolate_missing}
\alias{interpolate_missing}
\title{Fill missing values, and say that you did}
\usage{
interpolate_missing(
  data,
  value = NULL,
  method = c("linear", "locf", "none"),
  max_gap = 3
)
}
\arguments{
\item{data}{A panel with \code{iso3c} and \code{year}.}

\item{value}{Column(s) to fill (character). \code{NULL} fills every numeric column
except \code{year}.}

\item{method}{\code{"linear"} (default, interior gaps only), \code{"locf"} (carry the
last observation forward) or \code{"none"}.}

\item{max_gap}{Longest run of consecutive missing years to fill. Gaps longer
than this are left alone, because interpolating across a decade is not
interpolation. Default \code{3}.}
}
\value{
\code{data} with the gaps filled and, for each filled column, a logical
\verb{<column>_imputed} companion. The map verbs count those \emph{columns} when they
write provenance, so they keep working through any verb that preserves
columns. An \code{"countryatlas_imputed"} attribute lists the flag columns for
convenience, but nothing in the package reads it, and \code{dplyr} drops it as
it drops most attributes -- rely on the columns, not the attribute. Rows
come back sorted by \code{iso3c} then \code{year}.
}
\description{
Interpolate or carry forward missing observations in a panel. Every value this
invents is flagged in a companion column, and that flag is \strong{not optional} --
an imputed value that travels through a pipeline looking like data is exactly
the failure this package exists to prevent.
}
\section{The hard rule}{

The flag cannot be turned off. \code{\link[=world_map]{world_map()}} reads it and refuses to draw
imputed values as though they were observed without at least noting it in the
caption. If you need values with no flag, compute them yourself -- the
package will not hand you a frame where invented numbers are indistinguishable
from measured ones.
}

\examples{
p <- data.frame(iso3c = "USA", year = 2000:2005,
                gdp = c(1, NA, NA, 4, NA, 6))
interpolate_missing(p, "gdp")
}
\seealso{
\code{\link[=complete_years]{complete_years()}}, \code{\link[=rate_check]{rate_check()}}, \code{\link[=coverage_map]{coverage_map()}}
}
