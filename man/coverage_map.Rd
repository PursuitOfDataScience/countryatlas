% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/honest-maps.R
\name{coverage_map}
\alias{coverage_map}
\title{Map the data availability itself}
\usage{
coverage_map(data, value, by = NULL, title = NULL, ...)
}
\arguments{
\item{data}{A map-ready frame (polygon or \code{sf}).}

\item{value}{The column whose availability to map (unquoted).}

\item{by}{Optional grouping column for a panel: with a \code{year} column, use
\code{by = year} to see coverage change over time.}

\item{title}{Optional plot title (defaults to a generated one).}

\item{...}{Passed to \code{\link[=world_map]{world_map()}}.}
}
\value{
A \code{ggplot} object.
}
\description{
A choropleth of \emph{whether} a value is present, rather than what it is. The
companion to \code{\link[=audit_coverage]{audit_coverage()}}, which reports the same thing as a table: a
world map with a large well-covered region and a systematically empty one is
telling you something about the indicator that the headline map hides behind
a uniform grey.
}
\examples{
\donttest{
snap <- countryatlas::world_snapshot$countries
if (requireNamespace("maps", quietly = TRUE)) {
  attach_geometry(snap, geometry = "polygon") |>
    coverage_map(gdp_per_capita)
}
}
}
\seealso{
\code{\link[=audit_coverage]{audit_coverage()}}, \code{\link[=world_map]{world_map()}}
}
