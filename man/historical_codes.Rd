% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{historical_codes}
\alias{historical_codes}
\title{Historical / dissolved entities and their successor states}
\format{
A tibble with one row per (entity, successor):
\describe{
\item{historical}{Canonical name of the dissolved entity.}
\item{iso3c_hist}{The alpha-3 code the entity held at dissolution, where
one existed (\code{SUN}, \code{YUG}, \code{CSK}, \code{DDR}, \code{ANT}, \code{SCG}, \code{YMD}, ...); it
may since have been inherited by a successor (e.g. \code{YEM}).}
\item{dissolved}{Year the entity ceased to exist.}
\item{iso3c, country}{The successor state.}
\item{relation}{How the successor relates to the entity, per successor:
\code{"succession"} if it is a genuinely new state created at \code{dissolved}, or
\code{"continuation"} if the same state carried on (possibly with less
territory) or a state that already existed absorbed the entity. Sudan is
both at once -- \code{SDN} continued and \code{SSD} is new -- which is why the
relation is per successor rather than per entity. The distinction
matters: a \code{"continuation"} was \emph{not} created at \code{dissolved}, so testing
its data against that year says nothing, and the code did not cease to
exist either. \code{\link[=audit_time_coverage]{audit_time_coverage()}} keys on this column for both
reasons.}
}
}
\source{
Curated from ISO 3166-3 and the historical record.
}
\usage{
historical_codes
}
\description{
A curated crosswalk from dissolved entities (Soviet Union, Yugoslavia,
Czechoslovakia, ...) to the modern states that succeeded them -- one row per
(entity, successor) pair, dated, so historical panels can be brought onto
the modern ISO spine honestly instead of being silently dropped (or worse:
\code{countrycode} resolves \code{"USSR"} to Russia alone). Consumed by
\code{\link[=dissolve_country]{dissolve_country()}} and flagged by \code{\link[=check_country_match]{check_country_match()}}.
}
\details{
Kosovo (\code{XKX}) is included among the Yugoslavia and Serbia-and-Montenegro
successors on a \emph{territory} basis (its territory was part of both); filter
it out if your analysis follows strict UN-membership succession.
}
\keyword{datasets}
