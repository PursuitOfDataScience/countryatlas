% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rates.R
\name{to_ppp}
\alias{to_ppp}
\title{Convert to purchasing-power-parity terms}
\usage{
to_ppp(data, value, factor = NULL, suffix = "_ppp")
}
\arguments{
\item{data}{A panel with \code{iso3c}, \code{year} and the value column.}

\item{value}{The local-currency value column (unquoted).}

\item{factor}{Either a column holding the PPP conversion factor (unquoted),
or \code{NULL} to fetch the World Bank's (\code{PA.NUS.PPP}). Fetching needs the
network.}

\item{suffix}{Suffix for the new column (default \code{"_ppp"}).}
}
\value{
\code{data} with the PPP-converted column added.
}
\description{
Market exchange rates do not equalise what money buys. \code{to_ppp()} divides a
local-currency series by the PPP conversion factor, putting every country on
comparable international dollars -- the correction that makes a cross-country
\emph{level} comparison meaningful.
}
\examples{
d <- data.frame(iso3c = c("IND", "USA"), year = 2020L,
                gdp_lcu = c(1e5, 1e4), ppp = c(21.9, 1))
to_ppp(d, gdp_lcu, factor = ppp)
}
\seealso{
\code{\link[=deflate]{deflate()}}, \code{\link[=per_capita]{per_capita()}}
}
