% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rates.R
\name{deflate}
\alias{deflate}
\title{Convert a money series to constant prices}
\usage{
deflate(data, value, base_year, deflator = NULL, suffix = "_real")
}
\arguments{
\item{data}{A panel with \code{iso3c}, \code{year} and the value column.}

\item{value}{The nominal value column (unquoted).}

\item{base_year}{The year whose prices to express everything in.}

\item{deflator}{Either a column in \code{data} holding the price index (unquoted),
or \code{NULL} to fetch the World Bank GDP deflator (\code{NY.GDP.DEFL.ZS}) for the
countries and years present. Fetching needs the network.}

\item{suffix}{Suffix for the new column (default \code{"_real"}).}
}
\value{
\code{data} with the constant-price column added.
}
\description{
A nominal series is not comparable across years. \code{deflate()} divides by a
price index rebased to \code{base_year}, turning current-price values into constant
\code{base_year} prices.
}
\section{Which deflator}{

The GDP deflator is the right default for aggregate output. For household
spending the CPI (\code{FP.CPI.TOTL}) is usually preferred, and for cross-country
\emph{level} comparisons a deflator is not enough at all -- you want
\code{\link[=to_ppp]{to_ppp()}} as well, because exchange rates do not equalise purchasing power.
Deflating and converting to PPP are different corrections for different
problems, and a cross-country panel over time generally needs both.
}

\examples{
d <- data.frame(iso3c = "USA", year = 2000:2002,
                gdp = c(100, 110, 120), defl = c(90, 100, 105))
deflate(d, gdp, base_year = 2001, deflator = defl)
}
\seealso{
\code{\link[=to_ppp]{to_ppp()}}, \code{\link[=per_capita]{per_capita()}}, \code{\link[=index_to]{index_to()}}
}
