% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualization.R
\name{geom_country_labels}
\alias{geom_country_labels}
\title{Centroid-anchored country labels}
\usage{
geom_country_labels(
  mapping = NULL,
  data = NULL,
  repel = TRUE,
  flag = FALSE,
  size = 3,
  ...
)
}
\arguments{
\item{mapping}{Aesthetic mapping; defaults to \code{aes(label = iso3c)}.}

\item{data}{Optional layer data, as for any \code{ggplot2} geom: a frame (label
only those countries -- the usual way to label a handful rather than all
two hundred), or a function of the plot's data. Whatever you pass is
reduced to one centroid per country before it is drawn. Defaults to the
plot's own data.}

\item{repel}{Use \code{ggrepel} to avoid overlaps (default \code{TRUE}). Falls back to
plain labels, with a one-time note, when \code{ggrepel} is not installed.}

\item{flag}{If \code{TRUE}, label with flag emoji instead of the mapped label.}

\item{size}{Label text size.}

\item{...}{Passed to the underlying text geom.}
}
\value{
A \code{ggplot2} layer.
}
\description{
A \code{ggplot2} layer that places labels (names, ISO codes or flag emoji) at
country centroids, with optional \code{ggrepel} collision avoidance. Designed for
the polygon backend produced by \code{\link[=world_data]{world_data()}} / \code{\link[=join_world]{join_world()}}: it reads the
\code{long}, \code{lat} and \code{group} columns, so it errors on an \code{sf} frame and points at
\code{\link[ggplot2:ggsf]{ggplot2::geom_sf_text()}} instead. Placement is exact only while \code{group} is
present -- that is what identifies each country's separate pieces, and the
label goes on the largest one.
}
\examples{
\donttest{
library(ggplot2)
snap <- countryatlas::world_snapshot$countries
if (requireNamespace("maps", quietly = TRUE)) {
  mapdf <- attach_geometry(snap, geometry = "polygon")

  # Labelling all 188 countries at once is unreadable, and ggrepel responds
  # by dropping nearly every label. Pass `data` to choose a subset ...
  world_map(mapdf, gdp_per_capita) +
    geom_country_labels(
      data = ~ dplyr::filter(.x, iso3c \%in\% c("USA", "BRA", "CHN", "IND", "ZAF"))
    )

  # ... or zoom in, where there is room for every label.
  europe <- attach_geometry(
    dplyr::filter(snap, continent == "Europe"), geometry = "polygon")
  world_map(europe, gdp_per_capita) +
    geom_country_labels(size = 2.5) +
    coord_quickmap(xlim = c(-25, 45), ylim = c(34, 72))
}
}
}
