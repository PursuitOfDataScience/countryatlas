% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualization.R
\name{spin_globe}
\alias{spin_globe}
\title{Spin the globe}
\usage{
spin_globe(
  data,
  fill,
  lat = 20,
  n_frames = 60,
  fps = 15,
  backend = c("polygon", "sf"),
  width = 480,
  height = 480,
  file = NULL,
  ...
)
}
\arguments{
\item{data}{A map-ready frame (see \code{\link[=globe_map]{globe_map()}}): a country-level frame with
\code{iso3c} for the \code{"polygon"} backend, or an \code{sf} frame for \code{"sf"}.}

\item{fill}{The fill column (unquoted).}

\item{lat}{The latitude the globe is tilted toward (the viewer's eye line).}

\item{n_frames}{Number of frames in one full 360 degrees rotation.}

\item{fps}{Frames per second of the output animation.}

\item{backend}{\code{"polygon"} (default; needs \code{maps} + \code{mapproj}, no \code{sf}) or
\code{"sf"}.}

\item{width, height}{Pixel dimensions of the animation.}

\item{file}{Optional output path (\code{.gif}); a temporary file is used if \code{NULL}.}

\item{...}{Passed to \code{\link[=globe_map]{globe_map()}} (e.g. \code{fill} \code{style}, \code{palette}).}
}
\value{
The path to the written GIF, invisibly.
}
\description{
An animated GIF of the world rotating on its axis: a sequence of orthographic
\code{\link[=globe_map]{globe_map()}} frames at evenly spaced central longitudes, assembled into a
looping animation with the optional \code{gifski} (preferred) or \code{magick} package.
Embeds directly in R Markdown / Quarto / a README.
}
\examples{
# Six frames rather than the default 60, so this stays quick enough to be
# checked: \dontrun{} meant the example was never executed by anything, and
# an example nothing runs is an example free to rot.
\donttest{
if (requireNamespace("maps", quietly = TRUE) &&
    requireNamespace("mapproj", quietly = TRUE) &&
    (requireNamespace("gifski", quietly = TRUE) ||
     requireNamespace("magick", quietly = TRUE))) {
  # No sf required on the polygon backend.
  gif <- spin_globe(world_snapshot$countries, continent,
                    backend = "polygon", style = "categorical",
                    n_frames = 6, width = 200, height = 200)
  file.exists(gif)   # written to a temporary file
}
}
}
