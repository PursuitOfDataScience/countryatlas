% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sources.R
\name{register_country_source}
\alias{register_country_source}
\title{Register a data source on the country spine}
\usage{
register_country_source(
  name,
  fetch,
  meta = NULL,
  citation = NULL,
  key_col = "iso3c",
  key_type = "iso3c",
  cache = TRUE
)
}
\arguments{
\item{name}{Short source name, used everywhere else (e.g. \code{"wdi"}).}

\item{fetch}{A function \verb{(indicator, countries, years)} returning a tidy data
frame. See the contract below.}

\item{meta}{Optional one-line description of the provider.}

\item{citation}{Optional citation string; surfaced by \code{\link[=country_sources]{country_sources()}} so
users can credit the data.}

\item{key_col}{The country-key column \code{fetch} returns (default \code{"iso3c"}).
A column \emph{name}, not a coding scheme.}

\item{key_type}{How to read that column: any \code{\link[countrycode:countrycode]{countrycode::countrycode()}}
origin scheme (default \code{"iso3c"}). Set it to \code{"country.name"} for a source
keyed on country names.}

\item{cache}{Whether results should be memoised for the session
(default \code{TRUE}).}
}
\value{
Invisibly, the source name.
}
\description{
Teach \code{countryatlas} where to get indicators from. A source is a name plus a
\code{fetch} function; once registered it works with \code{\link[=fetch_indicator]{fetch_indicator()}},
\code{\link[=add_indicator]{add_indicator()}} and \code{\link[=compare_sources]{compare_sources()}} exactly like the built-in ones.
}
\details{
This is deliberately a registry rather than more \code{Suggests}. It means a
provider with no CRAN package -- V-Dem, the IMF, the World Inequality
Database -- is reachable without this package depending on anything, and it
means an internal or proprietary feed is a first-class citizen.
}
\section{The fetch contract}{

\code{fetch(indicator, countries, years)} must return a data frame with:
\itemize{
\item a country key column (named \code{key_col}, holding \code{key_type} values --
normally an \code{iso3c} column of \code{iso3c} codes),
\item a \code{year} column (integer) \emph{if} the data is a panel,
\item one column per requested indicator, named after the indicator, and
\item one row per key -- \code{iso3c} for a cross-section, \code{iso3c} and \code{year} for a
panel.
}

The key has to be unique because \code{\link[=add_indicator]{add_indicator()}} joins on it: a repeated
key multiplies the caller's rows, so it is reported rather than joined in
silence. Aggregate before returning if the provider serves several rows per
country-year.

\code{countries} is a character vector of \code{iso3c} codes or \code{NULL} for all;
\code{years} is a numeric vector or \code{NULL} for the provider's default. Returning
extra columns is fine -- they travel through. Anything the provider cannot
serve should come back as \code{NA}, not as a missing row, wherever that is
cheap to arrange.
}

\examples{
# A trivial in-memory source
register_country_source(
  "demo",
  fetch = function(indicator, countries = NULL, years = NULL) {
    data.frame(iso3c = c("USA", "FRA"), year = 2020L, demo_value = c(1, 2))
  },
  meta = "A toy source for the examples"
)
country_sources()
fetch_indicator("demo", "demo_value")

# A source keyed on country names rather than codes: `key_col` says which
# column holds the key, `key_type` says how to read it.
register_country_source(
  "demo_named",
  fetch = function(indicator, countries = NULL, years = NULL) {
    data.frame(country = c("United States", "Japan"), year = 2020L,
               demo_value = c(3, 4))
  },
  key_col = "country", key_type = "country.name",
  meta = "A toy name-keyed source"
)
fetch_indicator("demo_named", "demo_value")
}
\seealso{
\code{\link[=country_sources]{country_sources()}}, \code{\link[=fetch_indicator]{fetch_indicator()}}, \code{\link[=compare_sources]{compare_sources()}}
}
