% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/honest-maps.R
\name{value_by_alpha_map}
\alias{value_by_alpha_map}
\title{Value-by-alpha: equalise a rate by its denominator}
\usage{
value_by_alpha_map(
  data,
  value,
  equalize,
  style = c("quantile", "continuous", "binned", "jenks"),
  palette = NULL,
  n_bins = 5,
  alpha_range = c(0.15, 1),
  transform = c("rank", "log10", "identity"),
  background = "grey20",
  title = NULL,
  legend = NULL,
  projection = "equal_earth"
)
}
\arguments{
\item{data}{A map-ready frame (polygon or \code{sf}).}

\item{value}{The value column, carried by colour (unquoted).}

\item{equalize}{The equalising column, carried by opacity (unquoted) --
population, total counts, or whatever denominator the rate was built on.}

\item{style}{Classification for the colour channel (as \code{\link[=world_map]{world_map()}}).}

\item{palette}{Optional viridis palette name.}

\item{n_bins}{Number of colour bins for the binned styles.}

\item{alpha_range}{Minimum and maximum opacity (default \code{c(0.15, 1)}).}

\item{transform}{Transform applied to \code{equalize} before it is mapped to
opacity: \code{"rank"} (default, robust to the extreme skew of population),
\code{"log10"} or \code{"identity"}.}

\item{background}{Colour behind the countries, which shows through where
opacity is low (default a dark neutral).}

\item{title, legend}{Optional plot title and legend title.}

\item{projection}{Projection for the \code{sf} backend.}
}
\value{
A \code{ggplot} object.
}
\description{
A choropleth where colour carries the value and \strong{opacity} carries an
equalising variable (usually population), over a neutral background. It is
the answer to the small-number problem -- a rate computed over eleven
thousand people shouts as loudly as one computed over a billion -- and unlike
a cartogram it solves it \strong{without distorting geometry}, which is the main
objection to cartograms. Roth, Woodruff & Johnson (2010) introduced it for
exactly this purpose.
}
\examples{
\donttest{
snap <- countryatlas::world_snapshot$countries
if (requireNamespace("maps", quietly = TRUE)) {
  attach_geometry(snap, geometry = "polygon") |>
    value_by_alpha_map(gdp_per_capita, population)
}
}
}
\references{
Roth, R. E., Woodruff, A. W. & Johnson, Z. F. (2010). Value-by-alpha maps: an
alternative technique to the cartogram. \emph{The Cartographic Journal} 47(2),
130-140. \doi{10.1179/000870409X12488753453372}
}
\seealso{
\code{\link[=cartogram_map]{cartogram_map()}} and \code{\link[=dorling_map]{dorling_map()}} (the geometry-distorting
answers to the same problem), \code{\link[=world_map]{world_map()}}
}
