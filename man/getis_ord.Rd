% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial-stats.R
\name{getis_ord}
\alias{getis_ord}
\title{Getis-Ord G statistics (hot spots)}
\usage{
getis_ord(data, value, weights = NULL, local = TRUE)
}
\arguments{
\item{data}{A country-level frame with \code{iso3c} and the value column.}

\item{value}{The value column (unquoted).}

\item{weights}{A \code{\link[=country_weights]{country_weights()}} object. Defaults to land-border
contiguity, which excludes islands -- prefer \code{country_weights("knn")} for
global work.}

\item{local}{If \code{TRUE} (default) return the per-country \eqn{G_i^*} with
z-scores; if \code{FALSE} return the single global \eqn{G}.}
}
\value{
With \code{local = TRUE}, a tibble of \code{iso3c}, \code{gi_star}, \code{z_score} and
\code{p_value} (two-sided, from the normal approximation). With \code{local = FALSE},
a one-row tibble of \code{g}, \code{expected}, \code{n} and \code{n_links}.
}
\description{
Global \eqn{G} and local \eqn{G_i^*}: unlike Moran's I, these distinguish
clusters of \strong{high} values from clusters of \strong{low} ones, which is what
"hot spot" analysis usually wants.
}
\examples{
\donttest{
snap <- countryatlas::world_snapshot$countries
getis_ord(snap, gdp_per_capita, weights = country_weights("knn", k = 5))
}
}
\references{
Getis, A. & Ord, J. K. (1992). The analysis of spatial association by use of
distance statistics. \emph{Geographical Analysis} 24(3), 189-206.
\doi{10.1111/j.1538-4632.1992.tb00261.x}
}
\seealso{
\code{\link[=local_morans]{local_morans()}}, \code{\link[=country_weights]{country_weights()}}
}
