% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/time.R
\name{historical_geometry}
\alias{historical_geometry}
\title{Historical country boundaries}
\usage{
historical_geometry(year, dependencies = FALSE, projection = "equal_earth")
}
\arguments{
\item{year}{The year to draw, or a \code{Date} for a specific day. CShapes covers
1886-2019.}

\item{dependencies}{Include colonies and dependencies (default \code{FALSE},
matching \code{cshapes}). For any pre-decolonisation map you almost certainly
want \code{TRUE} -- most of Africa and Asia is otherwise absent.}

\item{projection}{Projection to return the geometry in (see \code{\link[=world_map]{world_map()}}),
or \code{NULL} for unprojected lon/lat.}
}
\value{
An \code{sf} frame with \code{gwcode}, \code{country}, \code{iso3c} (where one can be
assigned -- see below), \code{status}, \code{from}, \code{to} and geometry.
}
\description{
Country polygons as they were, from CShapes 2.0 (Schvitz et al. 2022), which
maps states \emph{and} colonies and dependencies for 1886-2019 with per-polygon
validity periods. A 1970 map with 2024 borders is a common and quiet error;
this is the fix.
}
\section{The ISO spine does not reach back}{

ISO 3166 was first published in 1974 and never covered colonies, so a
historical map cannot be keyed on \code{iso3c}. CShapes uses \strong{Gleditsch-Ward}
codes, which is why \code{gwcode} is the key here and \code{iso3c} is a best-effort
extra: it is \code{NA} for every entity that never had an ISO code (French West
Africa, the Gold Coast, the USSR before 1974). Join historical data on
\code{gwcode}, not on \code{iso3c}, and use \code{\link[=convert_country]{convert_country()}}\code{(to = "gwn")} to get
there from a modern code.
}

\examples{
\dontrun{
# Africa before decolonisation needs the dependencies
historical_geometry(1950, dependencies = TRUE)
}
}
\references{
Schvitz, G., Girardin, L., Ruegger, S., Weidmann, N. B., Cederman, L.-E. &
Gleditsch, K. S. (2022). Mapping the international system, 1886-2019: The
CShapes 2.0 dataset. \emph{Journal of Conflict Resolution} 66(1), 144-161.
\doi{10.1177/00220027211013563}
}
\seealso{
\code{\link[=world_geometry]{world_geometry()}}, \code{\link[=country_timeline]{country_timeline()}}, \code{\link[=world_map]{world_map()}}
}
