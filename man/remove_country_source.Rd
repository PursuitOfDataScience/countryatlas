% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sources.R
\name{remove_country_source}
\alias{remove_country_source}
\title{Remove a registered data source}
\usage{
remove_country_source(source)
}
\arguments{
\item{source}{One or more source names, as given to
\code{\link[=register_country_source]{register_country_source()}}.}
}
\value{
The names actually removed, invisibly.
}
\description{
The counterpart to \code{\link[=register_country_source]{register_country_source()}}. Registering is permanent for
the session, so without this there was no way to undo one -- which made any
code that registers a source (an example, a test, an exploratory script)
leave the registry permanently changed, and \code{\link[=country_sources]{country_sources()}} report
different rows afterwards.
}
\details{
The five built-in sources cannot be removed: they are what the package
documents, and dropping one would make \code{?fetch_indicator} wrong. Pass
\code{cache = FALSE} to a fetch instead if you want to bypass one.
}
\examples{
register_country_source("scratch", function(indicator, ...) NULL)
"scratch" \%in\% country_sources()$source
remove_country_source("scratch")
"scratch" \%in\% country_sources()$source
}
\seealso{
\code{\link[=register_country_source]{register_country_source()}}, \code{\link[=country_sources]{country_sources()}}
}
