% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/world_data.R
\name{country_data}
\alias{country_data}
\title{Lightweight one-row-per-country table}
\usage{
country_data(
  year,
  indicator = NULL,
  latest = FALSE,
  panel = FALSE,
  classify = c("income", "continent", "region"),
  cache = TRUE,
  language = "en",
  parallel = TRUE
)
}
\arguments{
\item{year}{A single year or a range (with \code{panel = TRUE}).}

\item{indicator}{A named character vector of WDI codes (or \code{NULL} for none).}

\item{latest}{Use the most recent non-\code{NA} value per country (single year).}

\item{panel}{Return a panel keyed on \code{iso3c} + \code{year} (implied when \code{year}
spans multiple years).}

\item{classify}{Which classifications to add.}

\item{cache}{Whether to use the WDI cache.}

\item{language}{WDI language code.}

\item{parallel}{Whether to fetch indicators in parallel. Ignored when the
cache is memory-only; see \code{\link[=world_data]{world_data()}}.}
}
\value{
A tibble, one row per country (or per country-year for a panel).

\code{iso3c} is the stable key; \code{country} is a \emph{label} and its spelling depends on
where the row came from. A successful fetch carries the World Bank's names
("Korea, Rep.", "Congo, Dem. Rep."), while the country spine used when the
fetch returns nothing carries the \code{countrycode} names ("South Korea",
"Congo - Kinshasa") -- as do \code{\link[=convert_country]{convert_country()}}, \code{\link[=standardize_country]{standardize_country()}}
and the rest of the package. Match on \code{iso3c}, and relabel with
\code{convert_country(iso3c, to = "country")} if you need one consistent set.
}
\description{
The analysis counterpart to \code{\link[=world_data]{world_data()}}: no polygons, one tidy row per
country (\code{iso3c}, \code{iso2c}, \code{country}, classifications and the requested
indicators). This is what you actually \code{join()} / \code{mutate()} / \code{summarise()}
/ \code{rank()} on; attach geometry only at draw time with \code{\link[=attach_geometry]{attach_geometry()}}.
}
\examples{
\donttest{
country_data(2020, c(co2 = "EN.GHG.CO2.MT.CE.AR5"))
}
}
