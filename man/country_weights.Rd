% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial-stats.R
\name{country_weights}
\alias{country_weights}
\title{Spatial weights on the country spine}
\usage{
country_weights(
  type = c("contiguity", "knn", "distance", "custom"),
  countries = NULL,
  k = 5,
  cutoff_km = NULL,
  w = NULL,
  style = c("W", "B"),
  scale = "small"
)
}
\arguments{
\item{type}{\itemize{
\item \code{"contiguity"} -- shared land border, from \code{\link[=country_borders]{country_borders()}}. Needs
\code{sf}. Islands get no neighbours; see \code{\link[=morans_i]{morans_i()}}'s note.
\itemize{
\item \code{"knn"} -- the \code{k} nearest countries by great-circle centroid distance.
Every country gets exactly \code{k} neighbours, islands included. Needs
nothing but the bundled \link{country_meta}.
\item \code{"distance"} -- every country within \code{cutoff_km}. Needs nothing.
\item \code{"custom"} -- your own adjacency (see \code{w}), which is how non-geographic
neighbourhoods -- trade volume, migration flows, colonial or language
ties -- go through the same API.
}
}}

\item{countries}{Optional \code{iso3c} vector to restrict the weights to. Defaults
to every country the chosen backend knows about.}

\item{k}{Neighbours per country for \code{type = "knn"} (default \code{5}).}

\item{cutoff_km}{Distance band for \code{type = "distance"}, in kilometres.}

\item{w}{For \code{type = "custom"}: either a square named matrix, or a long data
frame with columns \code{iso3c}, \code{neighbor} and optionally \code{weight}.}

\item{style}{\code{"W"} (default) row-standardises so each row sums to 1, the
usual choice for Moran's I; \code{"B"} leaves the weights binary/raw.}

\item{scale}{Natural Earth resolution for \code{type = "contiguity"}.}
}
\value{
A \code{countryatlas_weights} object: the weights matrix plus the scheme
that built it. Inspect it by printing; \code{as.matrix()} gives the matrix.
}
\description{
Build a reusable neighbour-weights object for \code{\link[=morans_i]{morans_i()}}, \code{\link[=local_morans]{local_morans()}},
\code{\link[=gearys_c]{gearys_c()}}, \code{\link[=getis_ord]{getis_ord()}} and \code{\link[=spatial_lag]{spatial_lag()}}. Four schemes, three of which
give every country at least one neighbour -- which land-border contiguity, the
historical default, cannot do for an island.
}
\section{Choosing a scheme}{

Contiguity encodes "shares a border", which is the right relation for
spillovers that cross borders by land. It is the wrong relation for a global
question, because it silently deletes the islands. \code{"knn"} is the safe
default for world-scale work: every country participates, and \code{k} controls how
local the neighbourhood is. \code{"distance"} is right when the process has a real
length scale. \code{"custom"} is right when geography is not the relevant space at
all.
}

\examples{
# k-nearest neighbours: no sf needed, and islands are included
w <- country_weights("knn", k = 4)
w

\donttest{
snap <- countryatlas::world_snapshot$countries
morans_i(snap, gdp_per_capita, weights = country_weights("knn", k = 5),
         n_perm = 99)
}
}
\seealso{
\code{\link[=morans_i]{morans_i()}}, \code{\link[=local_morans]{local_morans()}}, \code{\link[=lisa_map]{lisa_map()}}, \code{\link[=spatial_lag]{spatial_lag()}}
}
