% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/disputes.R
\name{dispute_policy}
\alias{dispute_policy}
\title{State which map convention you are using}
\usage{
dispute_policy(policy = NULL)
}
\arguments{
\item{policy}{One of:
\itemize{
\item \code{"none"} (default) -- no convention stated. Maps carry no dispute
annotation, exactly as before.
\item \code{"de_facto"} -- boundaries as administered on the ground, which is what
Natural Earth (and therefore this package's geometry) uses.
\item \code{"de_jure"} -- boundaries as claimed. The package does \strong{not} ship de
jure geometry; selecting this records the intent and warns that the
shapes drawn are still de facto.
\item \code{"neutral"} -- disputed areas marked as disputed rather than assigned.
}

Called with no argument, returns the current policy.}
}
\value{
The policy in effect, invisibly when setting.
}
\description{
Disputed territories are drawn differently by different conventions, and a
map that does not say which one it used is making a choice silently. This
sets the session's convention so \code{\link[=world_map]{world_map()}} can record it and
\code{\link[=map_provenance]{map_provenance()}} can report it.
}
\section{What this does and does not do}{

It records a choice and makes it visible. It does not redraw any boundary,
and selecting \code{"de_jure"} will not give you claimed-boundary geometry,
because the package does not have any -- Natural Earth's auxiliary claim
lines are not bundled. Anyone publishing under an institutional convention
should verify the shapes against that institution's own basemap rather than
trusting a setting.
}

\examples{
old <- dispute_policy()
dispute_policy("neutral")
dispute_policy()
dispute_policy(old)
}
\seealso{
\link{disputed_territories}, \code{\link[=check_dispute_coverage]{check_dispute_coverage()}}, \code{\link[=world_map]{world_map()}}
}
