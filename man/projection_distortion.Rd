% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/projections.R
\name{projection_distortion}
\alias{projection_distortion}
\title{Measure what a projection distorts}
\usage{
projection_distortion(
  projection = "equal_earth",
  measure = c("areal", "angular", "max_scale"),
  spacing = 10,
  max_lat = 85
)
}
\arguments{
\item{projection}{Projection to measure (see \code{\link[=projection_info]{projection_info()}}).}

\item{measure}{\code{"areal"} (default; projected area divided by true ground
area, so 1 is undistorted), \code{"angular"} (maximum angular deformation in
degrees, 0 for a conformal projection) or \code{"max_scale"} (the larger
principal scale factor).}

\item{spacing}{Grid spacing in degrees (default \code{10}).}

\item{max_lat}{Absolute latitude limit (default \code{85}).}
}
\value{
A tibble of \code{lon}, \code{lat} and \code{distortion}, with the area-weighted
mean and the range attached as the \code{"countryatlas_distortion"} attribute.
}
\description{
The numeric companion to \code{\link[=tissot_map]{tissot_map()}}: distortion sampled on a grid, so it
can be summarised, compared or mapped rather than eyeballed. Computed by
projecting a small circle at each grid point and measuring what happens to
it, which is the definition rather than an approximation of it.
}
\section{Reading the numbers}{

An equal-area projection has \code{"areal"} distortion of 1 everywhere -- that is
what equal-area \emph{means}, and it is worth checking rather than trusting.
A conformal projection has \code{"angular"} distortion of 0 everywhere and
unbounded areal distortion. A compromise projection is bad at both by a
little, everywhere, which is the trade it makes.
}

\examples{
\donttest{
if (requireNamespace("sf", quietly = TRUE)) {
  d <- projection_distortion("mercator", measure = "areal")
  attr(d, "countryatlas_distortion")
}
}
}
\seealso{
\code{\link[=tissot_map]{tissot_map()}}, \code{\link[=projection_info]{projection_info()}}, \code{\link[=projection_compare]{projection_compare()}}
}
