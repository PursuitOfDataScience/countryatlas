% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial-stats.R
\name{spatial_lag}
\alias{spatial_lag}
\title{The neighbour average, as a column}
\usage{
spatial_lag(data, value, weights = NULL, suffix = "_lag")
}
\arguments{
\item{data}{A country-level frame with \code{iso3c} and the value column.}

\item{value}{The value column (unquoted).}

\item{weights}{A \code{\link[=country_weights]{country_weights()}} object. Defaults to land-border
contiguity, which excludes islands -- prefer \code{country_weights("knn")} for
global work.}

\item{suffix}{Suffix for the new column (default \code{"_lag"}).}
}
\value{
\code{data} with the lagged column added. Countries the weights cannot
reach get \code{NA} -- and since that \code{NA} is indistinguishable from one caused
by a missing input value, the codes themselves are attached as the
\code{"countryatlas_excluded"} attribute, the frame-shaped counterpart to the
\code{excluded} column \code{\link[=morans_i]{morans_i()}} returns.
}
\description{
The spatially lagged value: for each country, the (weighted) mean of its
neighbours. The building block behind every statistic here, and useful on its
own -- "what is happening around this country" as a regressor, a map layer or
a scatter-plot axis against the country's own value (the Moran scatterplot).
}
\examples{
\donttest{
snap <- countryatlas::world_snapshot$countries
spatial_lag(snap, gdp_per_capita, weights = country_weights("knn", k = 5))
}
}
\seealso{
\code{\link[=country_weights]{country_weights()}}, \code{\link[=local_morans]{local_morans()}}
}
