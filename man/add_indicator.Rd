% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sources.R
\name{add_indicator}
\alias{add_indicator}
\title{Fetch an indicator and join it to your data}
\usage{
add_indicator(data, source, indicator, countries = NULL, years = NULL, ...)
}
\arguments{
\item{data}{A frame with \code{iso3c} (or a country column \code{\link[=join_world]{join_world()}} would
recognise).}

\item{source, indicator, countries, years, ...}{Passed to \code{\link[=fetch_indicator]{fetch_indicator()}}.
\code{countries} defaults to the codes already in \code{data}, so only what you need
is fetched.}
}
\value{
\code{data} with the indicator column(s) added.
}
\description{
\code{\link[=fetch_indicator]{fetch_indicator()}} plus \code{\link[=country_join]{country_join()}} in one step: pull an indicator from
any registered source and attach it to a frame you already have, matched on
the ISO spine (and on \code{year} too, when both sides are panels).
}
\examples{
\dontrun{
world_snapshot$countries |>
  add_indicator("wdi", c(unemployment = "SL.UEM.TOTL.ZS"), years = 2020)
}
}
\seealso{
\code{\link[=fetch_indicator]{fetch_indicator()}}, \code{\link[=country_join]{country_join()}}
}
