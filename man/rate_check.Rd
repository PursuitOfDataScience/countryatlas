% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rates.R
\name{rate_check}
\alias{rate_check}
\title{Flag rates computed over tiny denominators}
\usage{
rate_check(data, numerator, denominator, min_denominator = NULL, rate = NULL)
}
\arguments{
\item{data}{A country-level frame.}

\item{numerator, denominator}{The count and the population it is over
(unquoted).}

\item{min_denominator}{Denominators below this are flagged. \code{NULL} (default)
uses the 10th percentile of the observed denominators, which adapts to the
data rather than imposing a threshold that suits one indicator.}

\item{rate}{An existing rate column (unquoted), if you already computed it.
Otherwise the rate is \code{numerator / denominator}.}
}
\value{
A tibble of \code{iso3c}, \code{numerator}, \code{denominator}, \code{rate},
\code{expected_se} (the Poisson standard error of the rate, \eqn{\sqrt{r/d}}) and
\code{flagged}, sorted with the least reliable first.
}
\description{
The small-number problem, named: a rate over a very small population is mostly
noise, and on a map it shouts as loudly as a rate over a very large one. This
reports which countries' rates you should not trust before you plot them.
}
\section{What to do about it}{

Three answers, in rough order of preference: \code{\link[=smooth_rates]{smooth_rates()}} shrinks the
unreliable rates toward the global rate; \code{\link[=value_by_alpha_map]{value_by_alpha_map()}} leaves them
alone but fades them out; or drop them and say so in the caption. Plotting
them raw and unremarked is the one option that misleads.
}

\examples{
d <- data.frame(
  iso3c = c("CHN", "IND", "TUV", "NRU"),
  cases = c(50000, 42000, 3, 1),
  pop   = c(1.41e9, 1.39e9, 11000, 12000)
)
rate_check(d, cases, pop)
}
\references{
Roth, R. E., Woodruff, A. W. & Johnson, Z. F. (2010). Value-by-alpha maps: an
alternative technique to the cartogram. \emph{The Cartographic Journal} 47(2),
130-140. \doi{10.1179/000870409X12488753453372}
}
\seealso{
\code{\link[=smooth_rates]{smooth_rates()}}, \code{\link[=per_capita]{per_capita()}}, \code{\link[=value_by_alpha_map]{value_by_alpha_map()}}
}
