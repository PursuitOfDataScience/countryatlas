% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis.R
\name{aggregate_regions}
\alias{aggregate_regions}
\title{Roll countries up to region / income / continent}
\usage{
aggregate_regions(data, value, by = "region", fun = "sum", weight = NULL)
}
\arguments{
\item{data}{A country-level data frame.}

\item{value}{The value column to aggregate (unquoted).}

\item{by}{Grouping column(s) (character), default \code{"region"}. Combine with
\code{"year"} for panel roll-ups.}

\item{fun}{Aggregation: \code{"sum"} (default), \code{"mean"}, \code{"median"}, \code{"min"},
\code{"max"} or \code{"weighted_mean"}.}

\item{weight}{Optional weight column (unquoted) for \code{"weighted_mean"}.}
}
\value{
A tibble of \code{by} plus the aggregated value.
}
\description{
Aggregate a country-level value to a coarser grouping, optionally with
population-weighted means.
}
\section{Groups with no data}{

Missing values are dropped before aggregating, so a group is summarised from
whatever it does have. A group with \emph{no} non-missing value returns \code{NA}
rather than a figure: \code{sum()} would otherwise report \code{0}, \code{mean()} \code{NaN} and
\code{min()}/\code{max()} \code{-Inf}/\code{Inf}, each of which reads as a real total for a
region we simply have no data for. Use \code{\link[=audit_coverage]{audit_coverage()}} to see where those
gaps are.
}

\examples{
df <- data.frame(iso3c = c("USA", "CAN", "BRA"),
                 region = c("North America", "North America", "Latin America"),
                 gdp = c(21, 1.7, 1.4))
aggregate_regions(df, gdp, fun = "sum")
}
