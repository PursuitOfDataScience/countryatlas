% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/overrides.R
\name{wdj_overrides}
\alias{wdj_overrides}
\alias{country_overrides}
\title{Curated country-name overrides (replaces the silent drop-list)}
\usage{
wdj_overrides(extra = NULL)

country_overrides(extra = NULL)
}
\arguments{
\item{extra}{An optional named character vector of additional overrides
(names are country/region names, values are \code{iso3c} codes). Merged on top
of the built-in table, so you can extend or override it, e.g.
\code{wdj_overrides(c(Somaliland = "SOM"))}.}
}
\value{
A named character vector suitable for \code{countrycode(custom_match=)}.
}
\description{
A documented \code{custom_match} table for entities that map backends
(\code{\link[ggplot2:map_data]{ggplot2::map_data()}} and Natural Earth) get wrong or leave without an ISO
code. Earlier versions of the package \emph{deleted} these regions; now they are
\emph{matched} instead, so they stop silently disappearing from maps.

\code{country_overrides()} is the current name, as of the package's rename to
countryatlas. \strong{\code{wdj_overrides()} is deprecated} and warns once per session;
it returns the same table and will be removed. The help page kept describing
it as "a backward-compatible alias" after the code had started warning.
}
\details{
The table maps a country/region name (as spelled by the geometry backends) to
an ISO 3166-1 alpha-3 code. Pass the result as the \code{custom_match} argument to
\code{\link[=standardize_country]{standardize_country()}}, \code{\link[=world_data]{world_data()}} and friends. Every downstream code
(\code{iso2c}, continent, region, flag, ...) is derived from this \code{iso3c}, so a
single override is enough.
}
\section{Accented names and locales}{

Every name in this table is plain ASCII, and that is deliberate: ASCII
spellings match in any locale. Accented spellings (\code{"Curacao"} with a
cedilla, \code{"Saint Barthelemy"} with an acute) are matched natively by
\code{\link[countrycode:countrycode]{countrycode::countrycode()}} \emph{in a UTF-8 locale}, which is why they are not
listed here -- but in a non-UTF-8 locale (\code{LC_CTYPE=C}) they cannot be
compared reliably and resolve to \code{NA}.

If your input may contain accented country names, run in a UTF-8 locale.
De-accenting with \code{iconv(x, to = "ASCII//TRANSLIT")} gives ASCII spellings
that resolve everywhere, but it is not an escape from the locale problem:
\verb{//TRANSLIT} is itself locale-dependent, so under \code{LC_CTYPE=C} it returns
\code{NA} (or, given an explicit \code{from = "UTF-8"}, replaces each accent with \verb{?})
and nothing resolves. De-accent while still in a UTF-8 locale, or supply the
ASCII spellings directly.
}

\examples{
# `country_overrides()` is the current name; `wdj_overrides()` warns.
country_overrides()
country_overrides(c(Somaliland = "SOM"))
}
