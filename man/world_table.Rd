% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reporting.R
\name{world_table}
\alias{world_table}
\title{A publication-ready table from a map-ready frame}
\usage{
world_table(
  data,
  value = NULL,
  top_n = 20,
  desc = TRUE,
  columns = NULL,
  engine = c("gt", "tibble"),
  title = NULL,
  subtitle = NULL
)
}
\arguments{
\item{data}{A country-level or map-ready frame.}

\item{value}{The column to rank on (unquoted). \code{NULL} keeps every numeric
column, does not sort, and omits the \code{rank} column -- there is nothing to
rank by, and \code{top_n} then takes an arbitrary slice (it warns when it does).}

\item{top_n}{How many rows (default \code{20}). \code{Inf} for all.}

\item{desc}{Sort descending (default \code{TRUE}).}

\item{columns}{Extra columns to keep, beyond \code{iso3c}, \code{country} and \code{value}.}

\item{engine}{\code{"gt"} (default, if installed) or \code{"tibble"}.}

\item{title, subtitle}{Optional table title and subtitle (\code{gt} only).}
}
\value{
A \code{gt} table, or a tibble when \code{gt} is unavailable or
\code{engine = "tibble"}.
}
\description{
The tabular counterpart to \code{\link[=world_map]{world_map()}}: take the same curated frame and
produce a ranked, formatted table instead of a picture. Uses \code{gt} when it is
installed and a plain tibble otherwise, so it never becomes a hard dependency.
}
\examples{
world_table(countryatlas::world_snapshot$countries, gdp_per_capita,
            top_n = 5, engine = "tibble")
}
\seealso{
\code{\link[=rank_countries]{rank_countries()}}, \code{\link[=country_factsheet]{country_factsheet()}}, \code{\link[=world_map]{world_map()}}
}
